// script.js - Global JavaScript (if any)

// Example: Smooth scroll for anchor links (if you add any)
// document.querySelectorAll('a[href^="#"]').forEach(anchor => {
//     anchor.addEventListener('click', function (e) {
//         e.preventDefault();
//         document.querySelector(this.getAttribute('href')).scrollIntoView({
//             behavior: 'smooth'
//         });
//     });
// });

// You can add common utility functions here if needed by multiple tools,
// though for this project, most JS will be embedded in individual tool HTML files
// for easier copy-pasting and modularity as per the request.

// console.log("Global script.js loaded.");
